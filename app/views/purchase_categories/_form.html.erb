<%= form_with(model: purchase_category) do |form| %>
  <% if purchase_category.errors.any? %>
    <div style="color: red">
      <h2>
        <%= pluralize(purchase_category.errors.count, "error") %> prohibited this purchase_category
        from being saved:
      </h2>

      <ul>
        <% purchase_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :purchase_id, style: "display: block" %>
    <%= form.text_field :purchase_id %>
  </div>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
